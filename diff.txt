diff --git a/controllers/folderController.js b/controllers/folderController.js
index 794bf5e..8d42a3d 100644
--- a/controllers/folderController.js
+++ b/controllers/folderController.js
@@ -9,6 +9,11 @@ cloudinary.config({
 });
 
 async function createFolder(req, res, next) {
+  if (req.isAuthenticated()) {
+    next();
+  } else {
+    res.redirect("/");
+  }
   const errors = validationResult(req);
   if (!errors.isEmpty()) {
     res.render("pages/dashboard", { errors: errors.array(), showCreateFolder: true });
@@ -33,6 +38,11 @@ async function createFolder(req, res, next) {
 }
 
 async function updateFolder(req, res, next) {
+  if (req.isAuthenticated()) {
+    next();
+  } else {
+    res.redirect("/");
+  }
   const validatedFolder = matchedData(req);
 
   const folderId = req.params.folderId;
@@ -61,6 +71,11 @@ async function updateFolder(req, res, next) {
 }
 
 async function deleteFolder(req, res, next) {
+  if (req.isAuthenticated()) {
+    next();
+  } else {
+    res.redirect("/");
+  }
   const folderId = req.params.folderId;
   const files = await prisma.file.findMany({
     where: {
@@ -94,6 +109,11 @@ async function deleteFolder(req, res, next) {
 }
 
 async function renderForm(req, res) {
+  if (req.isAuthenticated()) {
+    next();
+  } else {
+    res.redirect("/");
+  }
   const folderId = req.params.folderId;
   let files;
   let folder;
@@ -140,6 +160,11 @@ async function renderForm(req, res) {
 }
 
 async function shareFolder(req, res) {
+  if (req.isAuthenticated()) {
+    next();
+  } else {
+    res.redirect("/");
+  }
   res.locals.homePath = true;
   const folderId = req.params.folderId;
   const sharedFolderUrl = `${req.protocol}://${req.get("host")}/folder/${folderId}/shared`;
diff --git a/package-lock.json b/package-lock.json
index fa27f2a..2f20a3e 100644
--- a/package-lock.json
+++ b/package-lock.json
@@ -15,6 +15,7 @@
         "@tailwindcss/cli": "^4.3.2",
         "bcryptjs": "^3.0.3",
         "cloudinary": "^2.10.0",
+        "cookie-parser": "^1.4.7",
         "date-fns": "^4.4.0",
         "dotenv": "^17.4.2",
         "ejs": "^6.0.1",
@@ -1419,6 +1420,25 @@
         "node": ">= 0.6"
       }
     },
+    "node_modules/cookie-parser": {
+      "version": "1.4.7",
+      "resolved": "https://registry.npmjs.org/cookie-parser/-/cookie-parser-1.4.7.tgz",
+      "integrity": "sha512-nGUvgXnotP3BsjiLX2ypbQnWoGUPIIfHQNZkkC668ntrzGWEZVW70HDEB1qnNGMicPje6EttlIgzo51YSwNQGw==",
+      "license": "MIT",
+      "dependencies": {
+        "cookie": "0.7.2",
+        "cookie-signature": "1.0.6"
+      },
+      "engines": {
+        "node": ">= 0.8.0"
+      }
+    },
+    "node_modules/cookie-parser/node_modules/cookie-signature": {
+      "version": "1.0.6",
+      "resolved": "https://registry.npmjs.org/cookie-signature/-/cookie-signature-1.0.6.tgz",
+      "integrity": "sha512-QADzlaHc8icV8I7vbaJXJwod9HWYp8uCqf1xa4OfNu1T7JVxQIrUgOWtHdNDtPiywmFbiS12VjotIXLrKM3orQ==",
+      "license": "MIT"
+    },
     "node_modules/cookie-signature": {
       "version": "1.2.2",
       "resolved": "https://registry.npmjs.org/cookie-signature/-/cookie-signature-1.2.2.tgz",
diff --git a/package.json b/package.json
index aff4224..23f8352 100644
--- a/package.json
+++ b/package.json
@@ -25,6 +25,7 @@
     "@tailwindcss/cli": "^4.3.2",
     "bcryptjs": "^3.0.3",
     "cloudinary": "^2.10.0",
+    "cookie-parser": "^1.4.7",
     "date-fns": "^4.4.0",
     "dotenv": "^17.4.2",
     "ejs": "^6.0.1",
