<% if (locals.errors) { %>
    <ul class="flex flex-col gap-5 text-center text-red-500 text-sm">
        <% errors.forEach(function(error) { %>
            <li>
                <%=error.msg%>
            </li>
            <% }); %>
    </ul>

    <%}%>